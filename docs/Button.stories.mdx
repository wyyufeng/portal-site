import { Meta, Story, Canvas,ArgsTable ,Source} from '@storybook/addon-docs/blocks';

import { Button,ButtonGroup } from '../packages/portal-components/src';

<Meta title="Button" component={Button} />

# Button

常用按钮组件

#### 默认
<Canvas>
  <Story name="Basic">
    <Button>Basic</Button>
  </Story>
</Canvas>

### 不同类型按钮
<Canvas>
  <Story name="Type">
    <ButtonGroup>
    <Button>Default</Button>
    <Button type="primary">Primary</Button>
  </ButtonGroup>
  </Story>
</Canvas>

### 不同大小按钮
<Canvas>
  <Story name="Size">
    <ButtonGroup>
    <Button type="primary">default</Button>
    <Button type="primary" size="md">md</Button>
    <Button type="primary" size="lg">lg</Button>
    <Button type="primary" size="full">full</Button>
    </ButtonGroup>
  </Story>
</Canvas>

### 不同状态按钮

<Canvas>
  <Story name="Status">
    <ButtonGroup>
    <Button disabled>disabled</Button>
    <Button type="primary" loading>loading</Button>
    </ButtonGroup>
  </Story>
</Canvas>

## API
<ArgsTable of={Button} />
