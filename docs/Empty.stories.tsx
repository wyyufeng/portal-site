import * as React from 'react';
import { Empty } from '../packages/portal-components/src';

export default {
  title: 'Empty',
  parameters: { component: Empty }
};
export const Base = () => <Empty></Empty>;
