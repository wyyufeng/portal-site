import { Meta, Story, Props, Preview, Source } from '@storybook/addon-docs/blocks';
import { Nav } from '../packages/portal-components';
import App from './App';

<Meta title="Portal-Components|Block-Nav" component={Nav} parameters={{ order: 1 }} />

# Nav

❗ **这个组件只能配合`portal-core`使用**

`Nav`组件将自动从 `portal-core`中获取当前路由表，并渲染成基本的导航栏.

### basic example

<Preview withSource="open">
    <Story height={200} name="nav">
        <App>
            <Nav />
        </App>
    </Story>
</Preview>

<Props of={Nav} />
