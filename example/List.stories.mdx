import { Meta, Story, Props, Preview, Source } from '@storybook/addon-docs/blocks';
import { List, Picture } from '../packages/portal-components';
import App from './App';

<Meta title="Portal-Components|Block-List" component={List} parameters={{ order: 1 }} />

# List

List 组件提供获取列表数据的能力，同时支持自定义列表项

### example

<Preview withSource="open">
    <Story name="简单列表">
        <App>
            <List resource="1" />
        </App>
    </Story>
</Preview>

<Preview withSource="open">
    <Story name="基础列表1">
        <App>
            <List
                resource="1"
                renderItem={(item) => {
                    return (
                        <List.Item>
                            <List.Item.Meta
                                title={item.title}
                                avatar={
                                    <span
                                        style={{
                                            width: 9,
                                            height: 9,
                                            border: '2px solid #39678d',
                                            borderRadius: '50%',
                                            display: 'inline-block',
                                            verticalAlign: 'middle'
                                        }}
                                    />
                                }
                            />
                            <div
                                style={{
                                    lineHeight: '30px',
                                    color: '#898989'
                                }}
                            >
                                {item.publishDate}
                            </div>
                        </List.Item>
                    );
                }}
            />
        </App>
    </Story>
</Preview>

<Preview withSource="open">
    <Story name="基础列表2">
        <App>
            <List
                resource="1"
                renderItem={(item) => {
                    return (
                        <List.Item>
                            <List.Item.Meta
                                title={item.title}
                                secondary={item.publishDate}
                                description={item.content.substr(0, 100)}
                                avatar={
                                    <Picture
                                        fallback={null}
                                        style={{
                                            width: 260,
                                            height: 160
                                        }}
                                    />
                                }
                                actions={
                                    <a
                                        style={{
                                            marginLeft: 10,
                                            textDecoration: 'none',
                                            color: '#0002'
                                        }}
                                        href="http://www.baidu.com"
                                    >
                                        查看详情
                                    </a>
                                }
                            />
                        </List.Item>
                    );
                }}
            />
        </App>
    </Story>
</Preview>

<Preview withSource="open">
    <Story name="基础列表3">
        <App>
            <List
                resource="1"
                renderItem={(item) => {
                    return (
                        <List.Item
                            extra={
                                <Picture
                                    style={{
                                        width: 260,
                                        height: 160
                                    }}
                                    source={item.imgSrc}
                                />
                            }
                        >
                            <List.Item.Meta
                                title={item.title}
                                secondary={item.publishDate}
                                description={item.content.substr(0, 100)}
                            />
                        </List.Item>
                    );
                }}
            />
        </App>
    </Story>
</Preview>

### List Props

<Props of={List} />

### List.Item Props

<Props of={List.Item} />

### List.Item.Meta Props

<Props of={List.Item.Meta} />
